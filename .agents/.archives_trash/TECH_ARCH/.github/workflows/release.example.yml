name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (example: v0.1.0)"
        required: true
        type: string
      title:
        description: "Release title (defaults to tag)"
        required: false
        type: string
      prerelease:
        description: "Mark as pre-release"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install build tooling
        run: python -m pip install --upgrade pip build

      - name: Build wheel and sdist
        run: python -m build --wheel --sdist

      - name: Upload dist artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/*

  publish-release:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Resolve release metadata
        id: meta
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${GITHUB_EVENT_NAME}" == "push" ]]; then
            tag="${GITHUB_REF_NAME}"
            title="${GITHUB_REF_NAME}"
            prerelease="false"
          else
            tag="${{ inputs.tag }}"
            title="${{ inputs.title }}"
            prerelease="${{ inputs.prerelease }}"
          fi

          if [[ ! "${tag}" =~ ^v[0-9]+(\.[0-9]+){2}([.-][0-9A-Za-z.-]+)?$ ]]; then
            echo "Tag '${tag}' must look like v1.2.3 (optionally with suffix)." >&2
            exit 1
          fi

          if [[ -z "${title}" ]]; then
            title="${tag}"
          fi

          echo "tag=${tag}" >> "${GITHUB_OUTPUT}"
          echo "title=${title}" >> "${GITHUB_OUTPUT}"
          echo "prerelease=${prerelease}" >> "${GITHUB_OUTPUT}"

      - name: Create or update GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail

          tag="${{ steps.meta.outputs.tag }}"
          title="${{ steps.meta.outputs.title }}"
          prerelease="${{ steps.meta.outputs.prerelease }}"

          if gh release view "${tag}" > /dev/null 2>&1; then
            gh release upload "${tag}" dist/* --clobber
          else
            args=(release create "${tag}" dist/* --title "${title}" --generate-notes --target "${GITHUB_SHA}")
            if [[ "${prerelease}" == "true" ]]; then
              args+=(--prerelease)
            fi
            gh "${args[@]}"
          fi
